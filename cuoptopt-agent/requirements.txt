# cuoptopt-agent + dashboard requirements
# Install with: pip3 install -r requirements.txt
#
# Python >= 3.10 required

# ── CLI ────────────────────────────────────────────────────────────────────
typer>=0.12
rich>=13

# ── LLM backends ──────────────────────────────────────────────────────────
anthropic>=0.28
openai>=1.30          # also used for NVIDIA NIM (OpenAI-compatible endpoint)

# ── Research ──────────────────────────────────────────────────────────────
scholarly>=1.7
arxiv>=2.1

# ── GitHub integration ────────────────────────────────────────────────────
PyGithub>=2.3

# ── Config & data models ──────────────────────────────────────────────────
pydantic>=2.7
PyYAML>=6.0

# ── Diff application ──────────────────────────────────────────────────────
unidiff>=0.7

# ── Dashboard (FastAPI + WebSocket server) ────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
aiosqlite>=0.20
