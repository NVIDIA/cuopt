# Headings That Must Be Flagged

Every heading below violates the title-case rule. `check_vale_rule.sh` asserts
Vale reports each one. If an edit to `Headings.yml` makes any of them pass, the
rule has been silently weakened -- most likely by an over-broad `exceptions`
entry, which makes Vale skip the whole heading.

## Process model

## cuOpt process model

## gRPC job states

## Using MPS or LP file directly
