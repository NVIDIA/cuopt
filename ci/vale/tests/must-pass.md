# Headings That Must Pass

Every heading below is correct title case. `check_vale_rule.sh` asserts Vale
reports none of them, so the rule cannot drift into demanding capitalized
prepositions ("Where To Find Examples") or mangling product names.

## Where to Find Examples

## Connect and Solve

## How mTLS Works

## Start the Server with TLS

## Job States
