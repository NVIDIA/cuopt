diff --git a/cpp/include/cuopt/linear_programming/mip/solver_settings.hpp b/cpp/include/cuopt/linear_programming/mip/solver_settings.hpp
index cad974d1a..288218da7 100644
--- a/cpp/include/cuopt/linear_programming/mip/solver_settings.hpp
+++ b/cpp/include/cuopt/linear_programming/mip/solver_settings.hpp
@@ -24,16 +24,6 @@
 
 namespace cuopt::linear_programming {
 
-struct benchmark_info_t {
-  double last_improvement_of_best_feasible    = 0;
-  double last_improvement_after_recombination = 0;
-  double objective_of_initial_population      = std::numeric_limits<double>::max();
-};
-
-// Forward declare solver_settings_t for friend class
-template <typename i_t, typename f_t>
-class solver_settings_t;
-
 template <typename i_t, typename f_t>
 class mip_solver_settings_t {
  public:
@@ -132,24 +122,18 @@ class mip_solver_settings_t {
   void set_mip_scaling(bool mip_scaling);
 
   /**
-   * @brief Set the benchmark info pointer
-   * This is used to extract various info from the solver during the run.
-   */
-  void set_benchmark_info_ptr(benchmark_info_t* benchmark_info_ptr) noexcept;
-
-  /**
-   * @brief Add an primal solution.
+   * @brief Set an primal solution.
    *
-   * @note This function can be called multiple times to add more solutions.
+   * @note Default value is all 0 or the LP optimal point.
    *
    * @param[in] initial_solution Device or host memory pointer to a floating point array of
    * size size.
    * cuOpt copies this data. Copy happens on the stream of the raft:handler passed to the problem.
    * @param size Size of the initial_solution array.
    */
-  void add_initial_solution(const f_t* initial_solution,
+  void set_initial_solution(const f_t* initial_solution,
                             i_t size,
-                            rmm::cuda_stream_view stream = rmm::cuda_stream_per_thread);
+                            rmm::cuda_stream_view stream = rmm::cuda_stream_default);
 
   /**
    * @brief Get the absolute tolerance.
@@ -217,12 +201,11 @@ class mip_solver_settings_t {
   bool get_log_to_console() const noexcept;
 
   /**
-   * @brief Get the vector of initial solutions. The solutions might be modified for internal
-   * representation(scaling, presolve etc.) compared to the added solutions.
+   * @brief Get the initial solution.
    *
-   * @return Initial solution vector std::vector<rmm::device_uvector<f_t>>
+   * @return Initial solution as a rmm::device_uvector<f_t>
    */
-  const std::vector<std::shared_ptr<rmm::device_uvector<f_t>>>& get_initial_solutions() const;
+  rmm::device_uvector<f_t>& get_initial_solution() const;
 
   /**
    * @brief Get incumbent solution callback
@@ -231,26 +214,14 @@ class mip_solver_settings_t {
    */
   internals::lp_incumbent_sol_callback_t* get_incumbent_solution_callback() const;
 
-  /**
-   * @brief Checks if initial solutions exists
-   *
-   * @return true if exists
-   */
-  bool has_initial_solutions() const;
-
-  /**
-   * @brief Get the benchmark info pointer
-   *
-   * @return benchmark info pointer
-   */
-  benchmark_info_t* get_benchmark_info_ptr() const noexcept;
+  bool has_initial_solution() const;
 
   struct tolerances_t {
     f_t absolute_tolerance    = 1.0e-4;
     f_t relative_tolerance    = 1.0e-6;
     f_t integrality_tolerance = 1.0e-5;
-    f_t absolute_mip_gap      = 1.0e-10;
-    f_t relative_mip_gap      = 1.0e-4;
+    f_t absolute_mip_gap      = 0.;
+    f_t relative_mip_gap      = 1.0e-3;
   };
 
   /**
@@ -269,13 +240,9 @@ class mip_solver_settings_t {
   bool log_to_console_  = true;
   std::string log_file_;
   /** Initial primal solution */
-  std::vector<std::shared_ptr<rmm::device_uvector<f_t>>> initial_solutions_;
+  std::shared_ptr<rmm::device_uvector<f_t>> initial_solution_;
   internals::lp_incumbent_sol_callback_t* incumbent_sol_callback_ = nullptr;
   bool mip_scaling_                                               = true;
-  // this is for extracting info from different places of the solver during benchmarks
-  benchmark_info_t* benchmark_info_ptr_ = nullptr;
-
-  friend class solver_settings_t<i_t, f_t>;
 };
 
 }  // namespace cuopt::linear_programming
diff --git a/cpp/include/cuopt/linear_programming/mip/solver_solution.hpp b/cpp/include/cuopt/linear_programming/mip/solver_solution.hpp
index 23fec33e5..ea263d17b 100644
--- a/cpp/include/cuopt/linear_programming/mip/solver_solution.hpp
+++ b/cpp/include/cuopt/linear_programming/mip/solver_solution.hpp
@@ -17,9 +17,6 @@
 
 #pragma once
 
-#include <cuopt/linear_programming/constants.h>
-#include <cuopt/linear_programming/utilities/internals.hpp>
-
 #include <rmm/cuda_stream_view.hpp>
 #include <rmm/device_uvector.hpp>
 
@@ -31,16 +28,16 @@
 
 namespace cuopt::linear_programming {
 
-enum class mip_termination_status_t : int8_t {
-  NoTermination = CUOPT_TERIMINATION_STATUS_NO_TERMINATION,
-  Optimal       = CUOPT_TERIMINATION_STATUS_OPTIMAL,
-  FeasibleFound = CUOPT_TERIMINATION_STATUS_FEASIBLE_FOUND,
-  Infeasible    = CUOPT_TERIMINATION_STATUS_INFEASIBLE,
-  Unbounded     = CUOPT_TERIMINATION_STATUS_UNBOUNDED
+enum class mip_termination_status_t {
+  NoTermination = 0,
+  Optimal       = 1,
+  FeasibleFound = 2,
+  Infeasible    = 3,
+  Unbounded     = 4,
 };
 
 template <typename i_t, typename f_t>
-class mip_solution_t : public base_solution_t {
+class mip_solution_t {
  public:
   mip_solution_t(rmm::device_uvector<f_t> solution,
                  std::vector<std::string> var_names,
@@ -53,13 +50,10 @@ class mip_solution_t : public base_solution_t {
                  f_t max_constraint_violation,
                  f_t max_int_violation,
                  f_t max_variable_bound_violation,
-                 i_t num_nodes,
-                 i_t num_simplex_iterations,
                  std::vector<rmm::device_uvector<f_t>> solution_pool = {});
 
   mip_solution_t(mip_termination_status_t termination_status, rmm::cuda_stream_view stream_view);
 
-  bool is_mip() const override { return true; }
   const rmm::device_uvector<f_t>& get_solution() const;
   rmm::device_uvector<f_t>& get_solution();
   f_t get_objective_value() const;
@@ -68,13 +62,9 @@ class mip_solution_t : public base_solution_t {
   double get_total_solve_time() const;
   double get_presolve_time() const;
   mip_termination_status_t get_termination_status() const;
-  static std::string get_termination_status_string(mip_termination_status_t termination_status);
-  std::string get_termination_status_string() const;
   f_t get_max_constraint_violation() const;
   f_t get_max_int_violation() const;
   f_t get_max_variable_bound_violation() const;
-  i_t get_num_nodes() const;
-  i_t get_num_simplex_iterations() const;
   const std::vector<std::string>& get_variable_names() const;
   const std::vector<rmm::device_uvector<f_t>>& get_solution_pool() const;
   void write_to_sol_file(std::string_view filename, rmm::cuda_stream_view stream_view) const;
@@ -91,8 +81,6 @@ class mip_solution_t : public base_solution_t {
   f_t max_constraint_violation_;
   f_t max_int_violation_;
   f_t max_variable_bound_violation_;
-  i_t num_nodes_;
-  i_t num_simplex_iterations_;
   std::vector<rmm::device_uvector<f_t>> solution_pool_;
 };
 
